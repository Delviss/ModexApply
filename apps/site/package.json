{
  "name": "@modex/site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The public build of the Modex Apply platform: the product itself, bundled to static files under docs/ and served at https://delviss.github.io/ModexApply/.",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check",
    "serve": "node build.mjs && node serve.mjs",
    "test": "node build.mjs --check",
    "test:browser": "node test/site.test.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "playwright": "1.56.0"
  }
}
